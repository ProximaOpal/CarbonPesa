# Dev requirements — runs on Windows without PostgreSQL or pg_config.
# Swap psycopg2-binary for the pure-Python aiosqlite/SQLite path.
# GeoAlchemy2 is excluded (not needed for SQLite fallback).
fastapi==0.109.2
uvicorn==0.27.1
SQLAlchemy==2.0.25
python-dotenv==1.0.1
pydantic==2.6.1
httpx>=0.27.0
requests==2.31.0
africastalking==2.0.2
cryptography>=42.0.0
